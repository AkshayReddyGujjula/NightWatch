/* GENERATED by scripts/gen-types.mjs from apps/contracts/schemas — do not edit. Run `npm run gen:types` after any contract change. */

export type DeadlineAtUtc = string | null;
export type ExpectedCandidates = string[];
export type Generation = number;
export type CandidateId = string;
export type SandboxId = string;
export type SessionHash = string;
export type Ready = ReadyWorld[];
export type ReleasedAtUtc = string | null;
export type RunId = string;
export type Status = "PENDING" | "RELEASED" | "TIMEOUT";
export type TimedOutAtUtc = string | null;
export type TimeoutSeconds = number;

/**
 * Barrier state returned by ready posts and ``GET .../browser-barrier`` (plan §12.2).
 */
export interface BrowserBarrierState {
  deadline_at_utc?: DeadlineAtUtc;
  expected_candidates: ExpectedCandidates;
  generation: Generation;
  ready?: Ready;
  released_at_utc?: ReleasedAtUtc;
  run_id: RunId;
  status: Status;
  timed_out_at_utc?: TimedOutAtUtc;
  timeout_seconds?: TimeoutSeconds;
}
/**
 * One candidate world that has registered readiness for a generation.
 */
export interface ReadyWorld {
  candidate_id: CandidateId;
  sandbox_id: SandboxId;
  session_hash: SessionHash;
}
