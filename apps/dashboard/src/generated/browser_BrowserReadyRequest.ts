/* GENERATED by scripts/gen-types.mjs from apps/contracts/schemas — do not edit. Run `npm run gen:types` after any contract change. */

export type CuaSessionId = string;
export type RunId = string;
export type SandboxId = string;
export type SessionHash = string;

/**
 * Body of ``POST /internal/worlds/{candidate_id}/browser-ready`` (plan §9.1).
 *
 * ``session_hash`` binds the readiness to one world session:
 * ``sha256("{run_id}|{candidate_id}|{sandbox_id}|{cua_session_id}")``.
 */
export interface BrowserReadyRequest {
  cua_session_id: CuaSessionId;
  run_id: RunId;
  sandbox_id: SandboxId;
  session_hash: SessionHash;
}
